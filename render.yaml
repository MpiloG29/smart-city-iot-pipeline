services:
- type: web
  name: smart-city-iot-pipeline
  runtime: docker
  dockerfilePath: ./Dockerfile
  plan: free
  autoDeploy: true
  envVars:
  - key: NODE_ENV
    value: production
  - key: PORT
    value: 3000
